{
  "name": "bank_webhook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "npx esbuild ./src/index.ts --bundle --platform=node --format=esm --target=node18 --outfile=dist/index.js --external:express --external:pg --external:dotenv",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@repo/db": "*",
    "@types/express": "^5.0.6",
    "dotenv": "^17.2.3",
    "esbuild": "^0.27.2",
    "express": "^5.2.1"
  }
}
